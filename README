## Prerequisites

- gfortran
- openscenegraph 3.6.5, openscenegraph-qt 3.5.7
- GSL (libgsl-dev on Ubuntu 22.04)
- GLM (libglm-dev on Ubuntu 22.04)
- Qt5

## Building

Before compiling vecds you should compile "FEMApp". Just change current directory
to the FEMApp and run `./makeallreleaseFEMApp.`



To compile vecds with cmake:

$ mkdir build
$ cd build
$ cmake ..
$ make

To run vecds with a GUI, go to the bin directory and run:

$ cd build
$ ./vecds

