
#ifndef __XML_CONSTANTS
#define __XML_CONSTANTS

enum XML_TYPES
{
    XML_NODE,
    XML_PARAMETER,
	XML_HEADER,
	XML_PREPROCESSING,
	XML_REMARK,
	XML_CDATA,
	XML_DOCTYPE,
	XML_TEXT,
	XML_UNKNOWN
};

#endif
